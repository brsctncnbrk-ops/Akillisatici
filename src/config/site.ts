// Site geneli özellik bayrakları.
// Entegrasyon hazır olmadan önce ilgili bölümü kapat; hazır olunca burayı güncelle.
export const siteConfig = {
  /** AdSense yayıncı kodu alındıktan sonra true yap. */
  adsenseEnabled: false,

  /** Bülten sağlayıcısı (Mailchimp/ConvertKit vb.) entegre edilince true yap. */
  newsletterEnabled: false,
};
